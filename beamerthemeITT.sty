% beamerthemeITT.sty — Utrecht University Beamer Theme
%
% Usage:  \usetheme{ITT}
%
% Options:
%   nl            — use Dutch logo (Universiteit Utrecht)
%   showlogo      — show small ITT logo on every content slide
%   nosectionpage — suppress automatic section divider slides
%
% Commands:
%   \standoutframe{Text}             — yellow emphasis slide
%   \thankframe{Title}{Contact}      — closing slide
%   \venue{Conference, City}         — enable conference title layout
%
% Logos are loaded automatically from logos/ based on the nl option.

\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{beamerthemeITT}[2026/03/17 A Beamer Theme for Utrecht University]

% -- Options -----------------------------------------------------------
\newif\if@ITTnosection\@ITTnosectionfalse
\newif\if@ITTnl\@ITTnlfalse
\newif\if@ITTshowlogo\@ITTshowlogofalse

\DeclareOption{nosectionpage}{\@ITTnosectiontrue}
\DeclareOption{nl}{\@ITTnltrue}
\DeclareOption{showlogo}{\@ITTshowlogotrue}
\ProcessOptions\relax

% -- Sub-themes ----------------------------------------------------------
\usecolortheme{ITT}
\useinnertheme{ITT}
\useoutertheme{ITT}

% -- Code listings --------------------------------------------------------
\RequirePackage{listings}
\lstset{
  basicstyle=\ttfamily\footnotesize,
  keywordstyle=\bfseries\color{ITTdarkblue},
  stringstyle=\color{ITTgreen},
  commentstyle=\itshape\color{ITTdarkgray},
  numberstyle=\tiny\color{ITTdarkgray},
  backgroundcolor=\color{ITTlightgray},
  frame=none,
  numbers=left,
  numbersep=8pt,
  breaklines=true,
  showstringspaces=false,
  tabsize=2,
  xleftmargin=1.5em,
}

% -- bibliography -----------------------------------------------------------
\usepackage[
  backend=biber,
  style=numeric,
  sorting=none
]{biblatex}

\addbibresource{bibliography.bib}

% -- Font setup -----------------------------------------------------------
\usepackage[T1]{fontenc}
\usepackage{soul}

\IfFileExists{merriweather.sty}{%
  \usepackage{merriweather}%
}{%
  \usepackage{palatino}%
}
\IfFileExists{opensans.sty}{%
  \usepackage[default]{opensans}%
}{}

\usefonttheme{professionalfonts}
\setbeamerfont{title}{family=\rmfamily, series=\bfseries, size=\LARGE}
\setbeamerfont{subtitle}{family=\sffamily, size=\normalsize}
\setbeamerfont{frametitle}{family=\rmfamily, series=\bfseries, size=\large}
\setbeamerfont{author}{family=\sffamily, size=\normalsize}
\setbeamerfont{institute}{family=\sffamily, size=\small}
\setbeamerfont{date}{family=\sffamily, size=\small}
\setbeamerfont{block title}{family=\sffamily, series=\bfseries, size=\normalsize}
\setbeamerfont{caption}{size=\footnotesize}
\setbeamerfont{footnote}{size=\tiny}

% -- Default logo paths (based on nl option) ---------------------------
\if@ITTnl
  \renewcommand{\@uulogopath}{images/ITT_logo_NL_RGB}
  \renewcommand{\@uulogoblackpath}{images/ITT_logo_NL_ZWART}
\else
  \renewcommand{\@uulogopath}{images/buonarroti}
  \renewcommand{\@uulogoblackpath}{images/buonarroti_icona}
\fi

% -- Numbered captions (Table 1, Figure 1, …) ---------------------------
\setbeamertemplate{caption}[numbered]

% -- Bibliography font size ---------------------------------------------
\setbeamerfont{bibliography item}{size=\small}
\setbeamerfont{bibliography entry author}{size=\small}
\setbeamerfont{bibliography entry title}{size=\small}
\setbeamerfont{bibliography entry location}{size=\small}
\setbeamerfont{bibliography entry note}{size=\small}

% -- Navigation symbols off ---------------------------------------------
\setbeamertemplate{navigation symbols}{}

% -- Hyperref colors ----------------------------------------------------
\hypersetup{
  colorlinks=true,
  linkcolor=ITTdarkblue,
  urlcolor=ITTpurple,
  citecolor=ITTdarkblue,
}

\setbeamercovered{transparent}

\endinput